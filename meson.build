project('zonegen',
        version: '2',
        license: 'GPL3',
        default_options: [
            'buildtype=plain',
            ]
        )

install_data(
    'init.d/zonegen',
    install_dir: '/etc/init.d/'
)

install_data(
    'src/zonegen',
    install_dir: '/bin/'
)
